spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/slt
    username: sltuser
    password: sltpass
    driver-class-name: org.postgresql.Driver

mybatis:
  configuration:
    map-underscore-to-camel-case: true

jwt:
  secret: "32바이트_이상_긴_문자열로_바꾸기_12345678901234567890"
  expMinutes: 60

server:
  port: 8080
